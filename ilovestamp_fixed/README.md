Шаг 1 — Установить Python 3.10

Обязательно версии 3.10, не 3.11 / 3.12.

Скачать здесь:

👉 https://www.python.org/downloads/release/python-3100/

При установке обязательно поставить галочку:

✔ Add Python to PATH

2️⃣ Шаг 2 — Скачать проект

Скачайте ZIP проекта и распакуйте, например, в:

C:\Users\<Имя>\Desktop\ilovestamp_fixed


Важно: внутри папок должна быть видна структура, например:

ilovestamp_fixed/app.py
ilovestamp_fixed/static/index.html
ilovestamp_fixed/utils/pdf_processing.py
ilovestamp_fixed/models/best.pt

3️⃣ Шаг 3 — Создать виртуальное окружение

Откройте PowerShell или VS Code терминал и выполните:

cd "C:\Users\<Имя>\Desktop\ilovestamp_fixed"
python -m venv .venv

4️⃣ Шаг 4 — Активировать окружение
.venv\Scripts\activate


Терминал должен стать таким:

(.venv) PS C:\Users\...\ilovestamp_fixed>

5️⃣ Шаг 5 — Установить зависимости
pip install -r requirements.txt


⚠ Это займёт пару минут, потому что будет ставиться PyMuPDF, Pillow и Ultralytics.

6️⃣ Шаг 6 — Проверить наличие модели YOLO

Убедитесь, что файл:

models/best.pt


находится внутри проекта.

Если нет — вставьте его туда.

7️⃣ Шаг 7 — Запустить backend сервер

В том же активированном окружении выполните:

python app.py


Если всё ок — появится сообщение:

Running on http://127.0.0.1:8000


Сервер не закрывать ⚠
Пусть работает в отдельном окне.

8️⃣ Шаг 8 — Открыть сайт (frontend)

Откройте новое окно терминала и выполните:

cd "C:\Users\<Имя>\Desktop\ilovestamp_fixed\static"
python -m http.server 5500


Если всё ок:

Serving HTTP on port 5500…


Теперь откройте браузер:

👉 http://127.0.0.1:5500

Это и есть интерфейс вашего сайта.

9️⃣ Готово!

Теперь сайт будет работать:

Загрузите PDF

Выберите режим

Нажмите кнопку

Получите результат 🎉

❗ Типичные ошибки и решения
❌ Python говорит pip не найден

Запускаешь так:

python -m pip install -r requirements.txt

❌ YOLO не видит модель

Проверь путь:

ilovestamp_fixed/models/best.pt

❌ Браузер пишет CORS error

Убедись, что API_BASE = "http://127.0.0.1:8000/api
" в script.js. 
iLoveStamp — умная фильтрация PDF по штампам, подписям и QR-кодам

Проект позволяет автоматически находить штампы, подписи и QR-коды в PDF, выделять их, а также создавать новые PDF, содержащие только нужные страницы или добавлять элементы поверх страниц.

Работает на YOLOv8, Flask, PyMuPDF и PIL.

🚀 Возможности
✔ Автоматическое определение:

штампов

подписей

QR-кодов

✔ Фильтрация PDF:

только штампы

только подписи

только QR

штамп + подпись

QR + подпись

штамп + подпись + QR (полный комплект)

страницы без элементов

✔ Выделение элементов:

прямоугольниками с отступом

толстой цветной линией

подписью над рамкой

✔ Добавление элементов на PDF:

добавление штампа

подписи

QR

на произвольные страницы

выбор угла размещения

📦 Установка
1. Клонируйте проект
git clone <repo-url>
cd ilovestamp_fixed

2. Создайте виртуальное окружение (Python 3.10)
python -m venv .venv

3. Активируйте окружение

Windows:

.venv\Scripts\activate

4. Установите зависимости
pip install -r requirements.txt

5. Поместите модель

Файл модели YOLO должен лежать здесь:

ilovestamp_fixed/models/best.pt

▶ Запуск backend (Flask)
python app.py


Сервер запустится на:

http://127.0.0.1:8000

▶ Запуск frontend (локальный web-сервер)

Перейдите в папку /static:

cd static
python -m http.server 5500


Откройте в браузере:

http://127.0.0.1:5500

📁 Структура проекта
ilovestamp_fixed/
│ app.py             — Flask API
│ requirements.txt
│ README.md
│
├── models/
│     └── best.pt    — YOLO модель
│
├── utils/
│     └── pdf_processing.py
│
└── static/
      ├── index.html
      ├── script.js
      └── style.css

🧠 Технологии

Python 3.10

Flask

PyMuPDF (fitz)

PyPDF2

PIL / Pillow

YOLOv8 (Ultralytics)

HTML / CSS / JS

⭐ Автор

iLoveStamp — интеллектуальная обработка PDF-документов.